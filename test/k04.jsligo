#import "common/check.jsligo" "Check"
#import "../lib/k04_bytes_type.jsligo" "Koan"

const test01 = (Check.check(Koan.concat([0x1234, 0x5678]) == 0x12345678));
const test02 = (Check.check(Koan.slice(0x1224) == 0x12));
const test03 = (Check.check(Koan.length(0x123456) == (3 as nat)));

// EOF