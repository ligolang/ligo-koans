#import "../lib/k01_native_types.jsligo" "Koan"

let test01 = (Koan.hello() == "Hello");
let test02 = (Koan.one() == 1);
let test03 = (Koan.one_nat() == (1 as nat));
let test04 = (Koan.one_tez() == (1 as tez));
let test05 = (Koan.true_value() == true);
let test06 = (Koan.increment_nat(1 as nat) == (2 as nat));

// EOF