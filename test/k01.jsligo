#import "common/check.jsligo" "Check"
#import "../lib/k01_native_types.jsligo" "Koan"

const test01 = (Check.check(Koan.hello() == "Hello"));
const test02 = (Check.check(Koan.one() == 1));
const test03 = (Check.check(Koan.one_nat() == (1 as nat)));
const test04 = (Check.check(Koan.one_tez() == (1 as tez)));
const test05 = (Check.check(Koan.true_value() == true));
const test06 = (Check.check(Koan.increment_nat(1 as nat) == (2 as nat)));

// EOF